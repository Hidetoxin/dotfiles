# vim: syntax=sh

# Login into `bw`
% bw, 2fa, pass, eval
eval "$(bw login "$(pass show 'self/bw/user')" "$(pass show 'self/bw/pass')" --code "$(2fa 'self/bw')" | rg 'export' | sed 's/\$ //g')"

# Search a `bw` item
% bw
bw list items --search <item>
