# vim: syntax=sh

# Stop macOS services
% macos, skhd, svim, yabai, sketchybar
brew services <action> svim         \
    && brew services <action> skhd  \
    && brew services <action> yabai \
    && brew services <action> sketchybar
