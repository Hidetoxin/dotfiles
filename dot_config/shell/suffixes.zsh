# Set `zsh` suffixes for handling files
alias -s md='treemd'
alias -s py='${EDITOR}'
alias -s json='jless'
alias -s bicep='${EDITOR}'
alias -s bicepparam='${EDITOR}'

# vim: et ts=4 sts=4 sw=4 syntax=sh
