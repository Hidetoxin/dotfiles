local icons <const> = require("confs.icons")
local fonts <const> = require("confs.fonts")
local colors <const> = require("confs.colors")
local defaults <const> = require("confs.defaults")

return {
	icons = icons,
	fonts = fonts,
	colors = colors,
	defaults = defaults,
}
