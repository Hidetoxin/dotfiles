# vim: syntax=yaml

{{- if (eq .chezmoi.hostname "mbpm4p") }}
github.com:
    user: "{{ (bitwarden "item" .bw.mbpm4p.id).identity.username }}"
    oauth_token: "{{ (bitwardenFields "item" .bw.mbpm4p.id).GitHubToken.value }}"
    git_protocol: "ssh"
{{- end }}
