# vim: syntax=yaml

{{- if (eq .chezmoi.hostname "mbpm4") }}
github.com:
    user: "{{ (bitwarden "item" .bw.mbpm4.id).identity.username }}"
    oauth_token: "{{ (bitwardenFields "item" .bw.mbpm4.id).GitHubToken.value }}"
    git_protocol: "ssh"
{{- end }}
